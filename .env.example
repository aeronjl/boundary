# Local development defaults to data/boundary.sqlite when these are unset.
# Set these for Turso/libSQL production or to target a remote database locally.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Optional local override.
DB_FILE_NAME=data/boundary.sqlite
